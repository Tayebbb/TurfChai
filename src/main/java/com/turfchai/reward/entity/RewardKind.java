package com.turfchai.reward.entity;

/** Matches the {@code ck_reward_kind} check constraint on {@code reward_products}. */
public enum RewardKind {
    WALLET_CREDIT,
    FREE_SLOT,
    DISCOUNT_NEXT,
    PRIORITY_PASS
}
