# Copy this file to .env and fill in your keys. .env is git-ignored.

# Primary LLM — get a key at https://openrouter.ai/keys (starts with "sk-or-")
OPENROUTER_API_KEY=

# Fallback LLM — get a token at https://huggingface.co/settings/tokens
HF_API_KEY=
